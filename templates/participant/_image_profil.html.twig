<div>
    {% if participant.filename %}
        <img src="{{ vich_uploader_asset(participant, 'imageFile')|imagine_filter('thumb') }}"
             style="width: 200px; height: auto">
    {% else %}
        <img src="{{ '/images/participants/User_Avatar.jpeg'|imagine_filter('thumb') }}"
             style="width: 200px; height: auto">

    {% endif %}
</div>