{% extends 'layout.html.twig' %}

{% block title %}Edit Participant{% endblock %}

    {% block main %}

            <div class="flex-column">
                <div class="text-center">
                    {{ include('participant/_image_profil.html.twig') }}
                </div>
                <h3 class="m-4 text-center">Mon profil</h3>
                    {% embed 'participant/_form.html.twig' %}
                        {% block buttons %}
                            <button type="submit" class="btn btn-primary float ml-4">Modifier</button>
                            <td><a class="btn btn-primary float ml-4" href="{{ path('sortie_index') }}">Retour</a></td>
                            <a href="{{ path('participant_show', {'id': participant.id}) }}"
                        {% endblock %}
                    {% endembed %}
            </div>

    {% endblock %}