{% extends 'layout.html.twig' %}

{% block main %}
        <h3 class="text-center">Annuler une sortie</h3>
        <div class="container">
            <table class="table table-bordered m-4">
                {{ form_start(form) }}
                <tr>
                    <td>Nom de la sortie:</td>
                    <td>{{ sortie.nom }}</td>
                </tr>
                <tr>
                    <td>Date de la sortie:</td>
                    <td>{{ sortie.dateDebut|date('d/m/Y') }}</td>
                </tr>
                <tr>
                    <td>Campus:</td>
                    <td>{{ sortie.campus.nom }}</td>
                </tr>
                <tr>
                    <td>Lieu:</td>
                    <td>{{ sortie.lieu.nom }}</td>
                </tr>
                <tr>
                    <td>Motif:</td>
                    <td class="p-0 m-0">
                        {{ form_errors (form.descriptionInfos) }}
                        {{ form_widget(form.descriptionInfos) }}</td>
                </tr>
            </table>
            <div class="text-center">
                <button class="btn btn-primary float" type="submit">Annuler la sortie</button>
                <a class="btn btn-primary float ml-4" href="{{ path('sortie_index') }}">Retour</a>
            </div>
            {{ form_end (form) }}


        </div>
{% endblock %}