{{ form_start(form) }}
<div class="container">

    <div class="row justify-content-around">
        <button class="btn btn-primary btn-lg float" name ="button" type="submit" value="enregistrer">{{ button_label|default('Enregistrer') }}</button>
        <button class="btn btn-primary btn-lg float" name ="button" type="submit" value="publier">{{ button_label|default('Publier la Sortie') }}</button>
    </div>
</div>

{{ form_end(form) }}

{{ form_start(form) }}
<div class="row">
<tr><td>
    <div class="col-6">{{ form_row(form.nom) }}</div>
</td>
<td>
    <div class="col-6">{{ form_row(form.campus) }}</div>
</td>
</tr>
</div>
<div class="row">
    <tr><td>
            <div class="col-6">{{ form_row(form.dateDebut) }}</div>
        </td>
        <td>
            <div class="col-6">{{ form_row(form.lieu) }}</div>
        </td>
    </tr>
</div>
<div class="row">
    <tr><td>
            <div class="col-6">{{ form_row(form.dateCloture) }}</div>
        </td>
        <td>
            <div class="col-6">{{ form_row(form.rue) }}</div>
        </td>
    </tr>
</div>
<div class="row">
    <tr><td>
            <div class="col-6">{{ form_row(form.nbInscriptionsMax) }}</div>
        </td>
        <td>
            <div class="col-6">{{ form_row(form.codePostal) }}</div>
        </td>
    </tr>
</div>
<div class="row">
    <tr><td>
            <div class="col-6">{{ form_row(form.duree) }}</div>
        </td>
        <td>
            <div class="col-6">Latitude</div>
        </td>
    </tr>
    <div class="row">
        <tr><td>
                <div class="col-6">{{ form_row(form.descriptionInfos) }}</div>
            </td>
            <td>
                <div class="col-6">Longitude</div>
            </td>
        </tr>
    </div>
</div>
<div class="row">
    <td></td>
<td class="align-items-center">
    <button class="btn btn-secondary">{{ button_label|default('S\'inscrire') }}</button>
</td>
    </div>
<div class="row justify-content-around">
    <button class="btn btn-primary btn-lg float" name ="button" type="submit" value="enregistrer">{{ button_label|default('Enregistrer') }}</button>
    <button class="btn btn-primary btn-lg float" name ="button" type="submit" value="publier">{{ button_label|default('Publier la Sortie') }}</button>
</div>
{{ form_end(form) }}