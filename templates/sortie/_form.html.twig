{{ form_start(form) }}
<div class="container">
    <div class="row">
        <div class="col">
            {{ form_row(form.nom) }}
            {{ form_row(form.dateDebut) }}
            {{ form_row(form.dateCloture) }}
            {{ form_row(form.nbInscriptionsMax) }}
            {{ form_row(form.duree) }}
            {{ form_row(form.descriptionInfos) }}
        </div>
        <div class="col-lg-5">
            {{ form_row(form.campus) }}
            {{ form_row(form.ville) }}
            {{ form_row(form.newVille) }}
            {{ form_row(form.lieu) }}
            {{ form_row(form.newLieu) }}
{#            <a id="rue">Rue : </a></br>
            <a>Code postal : </a><a id="codePostal"></a></br>
            <a id="latitude">Latitiude : </a></br>
            <a id="longitude">Longitude : </a></br>#}
        </div>
    </div>

    <div class="row justify-content-around">
            <button class="btn btn-primary btn-lg float" name ="button" value="enregistrer">{{ button_label|default('Enregistrer') }}</button>
            <button class="btn btn-primary btn-lg float" name ="button" value="publier">{{ button_label|default('Publier la Sortie') }}</button>
        {{ form_end(form) }}
        {% block buttons %}
        {% endblock buttons %}
        <a class="btn btn-primary btn-lg float" href="{{ path('sortie_index') }}">Retour</a>
    </div>
</div>

