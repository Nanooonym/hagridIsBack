{{ form_start(form) }}
<td>
    <div>{{ form_row(form.nom) }}</div>
</td>
<td>
    <div>{{ form_row(form.codePostal) }}</div>
</td>
<td class="align-items-center">
    <button class="btn btn-primary float">{{ button_label|default('Ajouter') }}</button>

{% if app.session.get('user') %}

    <input type="checkbox" class="checkbox" name="creerLieu">Créer un lieu</input>
</td>
{% endif %}
{{ form_end(form) }}
