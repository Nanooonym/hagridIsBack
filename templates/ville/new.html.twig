{% extends 'layout.html.twig' %}

{% block title %}Nouvelle ville{% endblock %}

{% block main %}
    <div class="container w-25">
        <div class="d-none d-md-block">
            <h3 class="text-center mb-5">Créer une ville</h3>

            {% embed 'ville/_form.html.twig' %}
                {% block buttons %}
                    <a class="btn btn-primary float ml-4" href="{{ path('sortie_index') }}">Retour</a>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
{% endblock %}