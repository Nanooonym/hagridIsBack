{% extends 'layout.html.twig' %}

{% block title %}Nouvelle ville{% endblock %}

{% block main %}
    <div class="flex-column">
        <h3 class="m-4 text-center">Nouvelle ville</h3>
        {% embed 'ville/_form.html.twig' %}
            {% block buttons %}
                <td><button type="submit" class="btn btn-primary float ml-4">Ajouter</button></td>
                <a class="btn btn-primary float ml-4" href="{{ path('sortie_index') }}">Retour</a>
            {% endblock %}
        {% endembed %}
    </div>
{% endblock %}