{% extends 'layout.html.twig' %}

{% block title %}Modifier le campus{% endblock %}

{% block main %}
    <div class="d-none d-md-block">
        <h3 class="text-center">Modifier le campus</h3>
        <div>
            <div class="row m-4">
                {{ include('campus/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
            <div class="container">
                <a href="{{ path('campus_index') }}" class="btn btn-primary float">Retour</a>
            </div>
        </div>
    </div>
{% endblock %}