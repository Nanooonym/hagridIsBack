<form method="post" action="{{ path('campus_delete', {'id': campu.id}) }}" onsubmit="return confirm('Etes-vous sûr de supprimer ce campus?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ campu.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
