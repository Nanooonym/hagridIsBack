{#<div class="row justify-content-between">#}
<div class="flex-fill flex-row justify-content-start">
    <div>
        <div style="float: left">
            <img class="d-none d-md-block" src="{{ asset('images/logo.jpg') }}" style="width: auto; height: 138px"
                 id="logo"></a>
        </div>
        <div style="float: left">
            <img class="d-block d-md-none" src="{{ asset('images/logo.jpg') }}" style="width: auto; height: 90px"
                 id="logo"></a>
        </div>
    </div>
    <div class="d-flex justify-content-start offset-5">
        <div class="d-none d-md-block">
            <div class="mt-3 float-center mr-3">
                <nav class="navbar navbar-expand-md navbar-light">
                    <div class="d-flex flex-column align-items-center">
                        <div>
                            <button class="navbar-toggler" type="button" data-toggle="collapse"
                                    data-target="#navbarSupportedContent"
                                    aria-controls="navbarSupportedContent" aria-expanded="false"
                                    aria-label="Toggle navigation">
                                <span class="navbar-toggler-icon"></span>
                            </button>
                            <div class="d-flex flex-column align-content-between">
                                <div class="collapse navbar-collapse mr-2" id="navbarSupportedContent">
                                    <ul class="navbar-nav" id="navbar">
                                        {% if 'ROLE_ADMIN' in app.user.roles %}
                                            {#                                <li class="nav-item">
                                                                                <a class="nav-link" href="{{ path('ville_index') }}">Villes</a>
                                                                            </li>
                                                                            <li class="nav-item">
                                                                                <a class="nav-link" href="{{ path('campus_index') }}">Campus</a>
                                                                            </li>#}
                                            <li class="nav-item">
                                                <a class="nav-link" href="{{ path('admin_home') }}">Admin</a>
                                            </li>
                                        {% endif %}
                                        <li class="nav-item">
                                            <a class="nav-link" href="{{ path('sortie_index') }}">Accueil</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link"
                                               href="{{ path('participant_edit', {'id': app.user.id}) }}">Mon
                                                Profil</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                        <div class="d-flex flex-row user">
                            {#                    <div class="col align-content-end">#}
                            <div>Date du jour : {{ "now"|date('d/m/Y') }}</div>
                            <div>Participant : {{ app.user.prenom }} {{ app.user.nom|upper|first }}.</div>
                            {#                    </div>#}
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</div>